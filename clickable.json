{
  "builder": "go",
  "kill": "textsecure",
  "ignore":[
    ".*",
    ".clickable",
    ".git",
    ".github",
    ".gitignore",
    ".gitmodules",
    "click",
    "docker",
    "main.go",
    "po",
    "scripts",
    "axolotl-web",
    "build",
    "output",
    "*.snap",
    "main",
    "output",
    "bind*.go",
    "*.syso",
    "*.go",
    "dist",
    ".flatpak-builder",
    ".idea",
    "flatpak",
    "libzkgroup*.so",
    "\\$GITHUB_WORKSPACE/*"
  ],
  "postbuild": "$ROOT/scripts/postbuild.sh $INSTALL_DIR; [ -z '$GOPATH' ] && cp /github/workspace/go/src/github.com/nanu-c/zkgroup/lib/libzkgroup_linux_$ARCH.so ${INSTALL_DIR}/lib/libzkgroup_linux_$ARCH.so; cp $GOPATH/src/github.com/nanu-c/zkgroup/lib/libzkgroup_linux_$ARCH.so ${INSTALL_DIR}/lib/libzkgroup_linux_$ARCH.so ||true; rm \\$GITHUB_WORKSPACE||true"
}
